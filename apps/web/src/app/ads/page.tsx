import { PageShell } from "@/components/page-shell";

export default function AdsPage() {
  return (
    <PageShell
      title="Ads"
      description="Ads with Final URL and Tracking Template managed as separate fields."
    />
  );
}
