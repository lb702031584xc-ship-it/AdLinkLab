import { PageShell } from "@/components/page-shell";

export default function UrlVersionsPage() {
  return (
    <PageShell
      title="URL Versions"
      description="Immutable URL version history (v1, v2, v3…) — never overwrite prior rows."
    />
  );
}
