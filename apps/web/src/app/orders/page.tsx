import { PageShell } from "@/components/page-shell";

export default function OrdersPage() {
  return (
    <PageShell
      title="Orders"
      description="Order records closing the Click → Conversion → Order chain."
    />
  );
}
