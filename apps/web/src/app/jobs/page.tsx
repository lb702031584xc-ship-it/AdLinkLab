import { PageShell } from "@/components/page-shell";

export default function JobsPage() {
  return (
    <PageShell
      title="Sync Jobs"
      description="BullMQ / SyncJob status for server-side urlChange, conversion upload, and related lab jobs — not the Google Ads Script scheduler. Script execution is controlled in Google Ads Scripts; see Script Integrations for Integration Admin."
    />
  );
}
