import { PageShell } from "@/components/page-shell";

export default function GoogleAccountsPage() {
  return (
    <PageShell
      title="Google Accounts"
      description="Linked Google Ads customer accounts (mock provider in Phase 0)."
    />
  );
}
