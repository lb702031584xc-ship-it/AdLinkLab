import { PageShell } from "@/components/page-shell";

export default function ClicksPage() {
  return (
    <PageShell
      title="Clicks"
      description="Captured click metadata including GCLID, UTM, IP, UA, and Referer (collection model only)."
    />
  );
}
