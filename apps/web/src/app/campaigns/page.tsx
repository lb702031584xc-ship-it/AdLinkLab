import { PageShell } from "@/components/page-shell";

export default function CampaignsPage() {
  return (
    <PageShell
      title="Campaigns"
      description="Campaign inventory synced through GoogleAdsProvider adapters."
    />
  );
}
