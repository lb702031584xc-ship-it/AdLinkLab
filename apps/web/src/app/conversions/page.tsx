import { PageShell } from "@/components/page-shell";

export default function ConversionsPage() {
  return (
    <PageShell
      title="Conversions"
      description="Conversion events for offline upload research via provider adapters."
    />
  );
}
